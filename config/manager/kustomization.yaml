resources:
- manager.yaml

generatorOptions:
  disableNameSuffixHash: true

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: quay.io/kuadrant/kcp-glbc
  newTag: latest

# Patches the controller deployment to add the webhooks port
# patchesStrategicMerge:
#   - patches/set_port.yaml